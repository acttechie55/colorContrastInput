import { ColorContrastDirective } from './color-contrast.directive';

describe('ColorContrastDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorContrastDirective();
    expect(directive).toBeTruthy();
  });
});
